# Archivo: config/pnc.yaml

# Parámetros fijos de la arquitectura y el dataset
fixed_params:
  height: 28
  width: 28
  n_classes: 10
  mixing: 'sum'
  batch_size: 128

# Cuadrícula de búsqueda (Grid Search)
grid_search:
  components: [5, 10, 15]
  lr: [0.01, 0.005]
  momentum: [0.9, 0.5]
