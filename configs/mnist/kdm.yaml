# ==========================================
# CELDA 2: DEFINICIÓN DE CONFIGURACIÓN (YAML)
# ==========================================
import yaml

config_content = {
    'modelo': 'KDM',
    'dataset': 'mnist',
    'arquitectura': {
        'input_dim': 784,
        'n_classes': 10
    },
    'grid_search': {
        'param_grid': {
            'units_dense': [64, 128],
            'learning_rate': [0.01, 0.001],
            'batch_size': [32, 64]
        },
        'cv_folds': 3 # K-Folds
    },
    'entrenamiento_final': {
        'epochs': 10
    }
}

os.makedirs("configs/mnist", exist_ok=True)
with open("configs/mnist/kdm.yaml", "w") as f:
    yaml.dump(config_content, f)

print("[✅] Archivo configs/mnist/kdm.yaml actualizado para el experimento.")
